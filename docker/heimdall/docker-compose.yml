version: "3.8"
services:
  heimdall:
    image: linuxserver/heimdall:latest
    container_name: heimdall
    # volumes:
    #   - ./config:/config
    ports:
      - 80:80
      - 443:443
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TIMEZONE}
    restart: unless-stopped
